# Use exact versions for dependencies
save-exact=true

# Use the latest registry
registry=https://registry.npmjs.org/

# Enable legacy peer dependencies to avoid issues with React 19
legacy-peer-deps=true
