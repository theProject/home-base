export const metadata = {
  title: "theProject. — Under Construction",
  description: "Pursuing the Relentless. Redefining the Possible.",
};

export default function MaintenanceLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
